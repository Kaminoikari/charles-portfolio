# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Playwright MCP browser automation artifacts
.playwright-mcp/

# Local visual-verification screenshots (delivered, not version-controlled)
docs/shot-*.png
.env*

# Voice pipeline intermediates (scripts/gen_voice_fish.py -> vc_to_tsumugi.py).
# Only the packed public/avatar/voice/*.m4a ship; the wavs are reproducible.
build/
scripts/__pycache__/

# Milfy 復刻管線（scripts/avatar）。只有原始碼進版控：
#   out/          建置產物，96MB，跑 make.py 就會重生
#   baseline.vrm  與已進版控的 public/avatar/mika-pink.vrm 位元組相同
#   mika-milfy.parts.json  建置用的部件對照表，執行期用不到
scripts/avatar/out/*
# ...除了這一個：它不是 make.py 產的，是手動跑 envelope.py 掃十支 .vrma 掃出
# 來的（而且要先有完成的模型才掃得動）。缺了它 envelope.load 回 None、裙子
# 少掉貼腿的外框，建置照樣成功不會警告。
!scripts/avatar/out/leg-envelope.json
scripts/avatar/__pycache__/
scripts/avatar/blender/__pycache__/
scripts/avatar/baseline.vrm
# public/avatar/mika-milfy-2.vrm 從 2026-09-03 起進版控並上線：它是網站的第三個
# 造型（avatarVariants.ts）。2026-08-31 曾決定只留本機，理由是「逆向復刻的模型
# 不該被公開下載」；使用者在 2026-09-03 推翻該決定，依據是管線的輸入端只有
# VRoid 官方範例模型與 18 張參考「圖片」，原作的模型檔從來不是輸入，所以檔案
# 裡沒有任何可還原成原作資料的東西。檔名帶 -2 是因為 /avatar/* 快取一年不可變，
# 位元組換了就得換名字（同日第二版：頭皮、共用膚色解、頸部帶）。
public/avatar/mika-milfy-2.parts.json

# delivery-record evidence for scripts/avatar (RESULT.txt cites these)
!scripts/avatar/evidence/*.log
