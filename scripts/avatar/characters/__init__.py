"""Character contracts: the values that belong to one character, not to the build.

One module per character, each holding the same names. `build.py` takes one as a
parameter and reads nothing from here directly, so adding a character is adding
a module rather than editing the pipeline.
"""
