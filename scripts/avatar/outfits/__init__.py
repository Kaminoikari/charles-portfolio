"""Outfit contracts: one module per garment package.

Each holds the package's own files, meshes, materials and colours, plus a
labelled FIT section for the numbers that are that package on one particular
body. `build.py` takes one as a parameter.
"""
