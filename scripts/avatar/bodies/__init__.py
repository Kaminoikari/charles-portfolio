"""Base-body contracts: what one export's own art already contains.

A body arrives with conventions the build has to read rather than choose. One
module per base body; `build.py` takes one as a parameter.
"""
